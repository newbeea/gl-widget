import { RenderableElement } from '../../../RenderableElement';
declare class SphereElement extends RenderableElement {
    data: any;
    constructor(material: any, options: any);
}
export { SphereElement };

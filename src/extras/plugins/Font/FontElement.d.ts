import { RenderableElement } from '../../../RenderableElement';
declare class FontElement extends RenderableElement {
    data: any;
    constructor(material: any, text: any, options: any);
}
export { FontElement };
